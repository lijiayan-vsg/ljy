# 课设提交清单

> 依据任务书「4. 具体要求 (6) 提交物清单」共 5 项。下表标注每项的完成状态，以及还需你手动操作的部分。

---

## 一、5 项提交物状态总览

| # | 提交物 | 状态 | 说明 |
|---|--------|------|------|
| ① | 代码仓库（git，含完整提交历史、运行脚本、readme） | ✅ 已完成 | 已 `git init` 并建立 8 次提交 + 5 个 tag（v0.1~v1.0） |
| ② | 需求规格说明书 | ✅ 已完成 | `需求规格说明书.md` |
| ③ | 设计报告 | ✅ 已完成 | `设计报告.md`（含 AI 使用披露章节） |
| ④ | 3 分钟演示视频 | ⏳ 待你录制 | 脚本已备好 `演示视频脚本.md` |
| ⑤ | 过程档案（prompt 日志、打卡记录） | ⚠️ 部分完成 | prompt 日志已有；微信打卡截图待你补充 |

---

## 二、仍需你手动完成的 3 件事

### 1. 录制 3 分钟演示视频（提交物 ④）

- 按 `演示视频脚本.md` 的流程录制，输出 MP4。
- 建议命名：`演示视频.mp4`，单独上传（视频较大，不入 git 仓库）。

### 2. 制作答辩 PPT（成果提交要求）

- 按 `答辩PPT大纲.md` 的 10 页结构制作。
- 重点准备「三大技术方向覆盖」页与「问答准备」里的 4 段代码讲解。

### 3. 补充微信打卡截图 + 推送到远程仓库

- 微信打卡记录截图整理后放入过程档案（不入 git 仓库，单独提交）。
- （可选）推送到 GitHub：`git remote add origin <仓库地址>` → `git push -u origin main --tags`。推送前请确认 `.gitignore` 已排除 `.claude/` 等本地配置，避免凭据泄露。

---

## 三、已入库到 git 仓库的文件（提交物 ①）

共 **54 个文件**，按模块分组：

| 分组 | 文件 |
|------|------|
| 顶层文档 | `readme.md`、`选题说明.md`、`方案设计.md`、`需求规格说明书.md`、`设计报告.md`、`学习笔记.md`、`答辩PPT大纲.md`、`演示视频脚本.md`、`制造智能技术课程设计任务书.pdf`、`requirements.txt`、`run.bat`、`.gitignore` |
| 数据 | `data/README.md`、`download_data.py`、`preprocess_msm.py`、`preprocess.py`、`processed/`（msm_features.csv、ai4i_processed.csv、scaler、index.json） |
| 算法 | `src/ga.py`、`classifier.py`、`rul.py`、`rag.py`、`knowledge_base.json`、`paths.py`、`README.md` |
| 后端 | `app/main.py`、`config.py`、`schemas.py`、`registry.py`、`inference.py`、`samples.py`、`db.py`、`routers/`、`README.md` |
| 前端 | `web/app.py`、`.streamlit/config.toml` |
| 测试 | `tests/test_api.py` |
| 模型指标 | `models/*.json`（评估指标与最优参数） |
| 提示词记录 | `prompt/*.json`（7 份） |

**未入库（已通过 .gitignore 排除）**：
- `models/*.joblib`（训练模型，可由脚本重建）
- `data/predictions.db*`（运行时历史库）
- `.claude/`（本地配置，可能含凭据）
- `data/raw/ai4i2020.csv`（AI4I 原始数据，提交链接）
- MSM 原始大数据（14.2 GB，通过 `data/download_data.py` 下载）

---

## 四、提交前自检

- [ ] `python tests/test_api.py` 全部通过（当前已通过）
- [ ] `run.bat` 可一键启动前后端
- [ ] 前端 5 页均正常显示、无空白
- [ ] 报告与代码中的指标数值一致（准确率 96.11%、F1 0.9444、R² 0.0933）
- [ ] 已准备好答辩 PPT 与 3 分钟视频
- [ ] git 提交历史与 tag 完整（`git log --oneline`、`git tag`）
