 学习笔记

 一、AI编程工具学习

本次课程设计采用vibecoding方法论，即通过自然语言与AI协作完成软件开发。

（一）vibecoding关键方法

第一，提示词工程。需要将复杂任务拆解为若干可执行的子任务，并为每个子任务明确约束条件（如技术栈、接口格式、数据库表结构）和验收标准，使AI生成的代码更贴合实际需求。第二，上下文工程。为项目建立统一的规则文件（如.cursorrules），在AI修改代码前先让其通读现有项目结构和代码风格，从而保持生成代码的一致性。第三，规格驱动开发。先撰写详细的需求规格说明书，再让AI根据规格制定分步实现计划，每一步完成后都对照验收标准进行验证，确保不偏离目标。第四，自动化测试与人工审查。对AI生成的代码须通过自动化测试脚本进行验证，并逐行审查逻辑正确性，避免引入隐藏缺陷。第五，调试与人工接管策略。当AI生成代码出现运行时错误时，采用回滚到上一个稳定版本、缩小本次改动范围、构造最小复现案例等方式高效定位和修复问题。

（二）拟选用的Harness工具

第一，Claude Code，作为本次开发的核心Harness工具。Claude Code是由Anthropic推出的深度编程辅助工具，具备强大的长上下文理解能力（200K token窗口），能够一次性处理整个项目代码库的上下文信息，这在进行系统级架构设计、跨模块代码重构以及复杂算法实现时具有显著优势。同时，Claude Code支持交互式对话式开发，我可以通过自然语言逐步描述需求，Claude Code能够基于对项目结构的理解生成符合现有代码风格的实现方案，并主动指出潜在的设计缺陷和边界条件问题。第二，GitHub Copilot，作为辅助代码补全工具，在编写重复性代码（如数据库CRUD操作、API路由定义、单元测试模板）时提高日常编码效率。第三，Cursor，作为备选工具，在Claude Code遇到特定任务处理不理想时切换使用，确保开发进度不受单点工具限制。以上工具将根据开发阶段灵活组合使用，核心算法模块开发和系统集成以Claude Code为主，常规功能开发以GitHub Copilot为辅，遇到瓶颈时切换Cursor进行补充。

（三）拟选用的大模型

第一，DeepSeek-v4，作为主要代码生成模型，编程能力强且推理速度快。第二，Claude Code内置的Claude模型，用于复杂架构设计和长上下文逻辑处理，由于其与Claude Code工具深度集成，在理解整个代码库的基础上进行代码生成和修改时效果最佳。

 二、Git版本控制学习

（一）Git核心原理

Git是一个分布式版本控制系统，记录的是每次提交时整个项目的完整快照而非文件间的差异。每个提交对象包含指向项目根目录树对象的指针、作者信息、提交说明和父提交指针，这种设计使分支操作极为轻量。Git中的文件有三种状态：已修改但未暂存、已暂存到暂存区、已提交到本地数据库。Git的对象模型包括Blob（文件内容快照）、Tree（目录结构快照）、Commit（提交记录）和Tag（里程碑标记）。理解这些底层概念有助于更好地管理代码历史。

（二）关键功能与使用计划

第一，严格执行小步提交策略，每次完成一个功能点即做一次提交。第二，提交信息按照规范书写，feat表示新增功能，fix表示修复bug，docs表示更新文档，test表示测试相关，refactor表示代码重构，保证提交历史的可读性。第三，建立main和dev两条主分支，开发新功能时从dev切出feature分支，完成后合并回dev。第四，在关键节点打上对应标签，如v0.1表示数据准备完成，v0.2表示后端API完成，v0.3表示算法模块完成，v0.4表示前端集成完成，v1.0表示完整demo就绪。


 三、选题调研

（一）选题方向

经过对多个制造场景的对比分析，最终确定选择"CNC刀具磨损预测与智能诊断系统"作为本次课程设计的开发主题。

（二）选择理由

第一，CNC机床是制造业最核心的加工设备，刀具磨损直接关系到加工质量和生产效率，场景具有极强的真实业务感。第二，GitHub上存在多个可直接参考的开源项目，如CutMind项目实现了基于LSTM的刀具磨损预测和RAG知识库检索，CNC-Tool-Wear-Detection项目提供了完整的数据预处理、特征工程和多种机器学习模型对比框架，可大幅降低开发风险。第三，公开可用的NASA铣削数据集和AI4I 2020预测性维护数据集均可轻松获取，数据格式清晰，便于快速验证算法。

（三）技术方向覆盖

第一，智能优化技术方面，采用遗传算法对LSTM或随机森林模型的超参数（学习率、树深度、隐藏层节点数）进行自动寻优，提升预测精度。第二，模式与图像识别方面，对采集到的振动、电流等多维传感器信号提取时域和频域特征，训练分类器识别正常切削、轻微磨损和严重磨损等不同状态。第三，知识工程方面，构建包含刀具磨损现象、常见原因和推荐解决方案的知识库，借助RAG检索增强生成技术，在检测到异常时自动推送诊断建议和维修方案。以上三个技术方向各自承担独立的业务功能，形成从状态识别到参数优化再到诊断推送的完整闭环。

（四）系统架构

整体采用B/S架构。前端使用Streamlit或Vue.js构建交互式看板，展示设备实时状态、磨损预测曲线和告警列表。后端使用FastAPI提供数据上传、模型推理和知识库查询等RESTful API。数据存储层采用SQLite或MySQL保存传感器历史数据和预测记录，同时使用ChromaDB向量数据库存储知识库的嵌入向量以支持语义检索。算法模块作为独立服务被后端调用，三者通过API接口通信，形成完整的数据流闭环。

（五）数据来源与预处理

从Kaggle下载NASA铣削数据集，该数据集包含多组不同切削条件下的传感器信号及其对应的刀具磨损测量值，同时补充AI4I 2020数据集中与设备健康度相关的特征以增强模型泛化能力。数据预处理环节包括缺失值填充、异常值剔除、信号滤波和归一化，以及针对时序数据的滑窗切分，处理后的数据存入数据库供后续训练和推理使用。


 