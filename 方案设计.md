# 方案设计

## 一、功能需求分析

本系统围绕刀具磨损全生命周期管理，设计以下核心功能模块：

（1）数据采集与管理模块。支持多传感器时序数据的上传、存储与管理，包括振动信号、声音信号、切削电流、机床控制器（MTConnect）数据等的批量导入和实时接入。

（2）刀具状态识别模块。对传感器数据进行预处理与特征提取，通过训练好的分类模型自动判断当前加工状态，输出「正常切削 / 加工异常 / 刀具缺陷」状态标签及置信度。

（3）剩余寿命预测模块。基于 LSTM 等时序预测模型，输入历史磨损数据序列，输出刀具剩余使用寿命（RUL）预测值及预测置信区间。

（4）智能诊断与推荐模块。当检测到异常或预测寿命低于阈值时，查询知识库并推送诊断建议、可能原因和推荐处置方案。

（5）可视化看板模块。提供设备状态总览、实时数据波形图、状态识别结果、寿命预测曲线、告警列表和诊断建议推送等交互界面。

## 二、方案论证

（1）技术路线可行性。多传感器特征提取结合机器学习分类器（随机森林/XGBoost）在刀具状态监测与加工异常检测方面已有广泛验证；LSTM 在处理时序数据方面具有成熟理论基础，适用于刀具磨损这类具有时序依赖性的预测任务；遗传算法作为经典的超参数优化方法，实现简单且收敛效果好；RAG 知识库检索结合向量数据库，能够实现高效、可扩展的智能问答与诊断推荐。三者技术成熟度均较高，可在 10 天开发周期内完成集成。

（2）数据资源可行性。选用 MSM 多传感器铣削数据集（用于状态识别与诊断）与 AI4I 2020 预测性维护数据集（用于寿命预测）两个公开数据集（详见下文数据集选择），数据格式清晰，可直接用于模型训练与验证。

（3）架构可行性。B/S 架构下，前端 Streamlit 或 Vue.js 生态成熟、开发效率高；后端 FastAPI 轻量高效，支持异步处理和自动 API 文档生成；算法模块可封装为独立的 Python 服务，通过 RESTful API 与后端通信，模块间解耦良好，便于分工开发和独立测试。

## 三、方案规划

### （一）技术栈

| 层级 | 技术选型 | 选型理由 |
|------|---------|---------|
| 前端 | Streamlit 或 Vue 3 + Element Plus | Streamlit 开发速度快，适合快速原型；Vue3+Element Plus 界面更专业，可视实际进度选择 |
| 后端 | FastAPI（Python） | 轻量高效、自动生成 API 文档、与 AI 模型（PyTorch/Scikit-learn）无缝集成 |
| 数据库 | SQLite（开发）/ MySQL（部署）+ ChromaDB | SQLite 轻量易用，适合 demo 阶段；ChromaDB 为向量数据库，支撑 RAG 知识库检索 |
| 算法框架 | PyTorch + Scikit-learn | PyTorch 支持 LSTM 等深度学习模型；Scikit-learn 提供随机森林等传统 ML 算法 |
| 版本控制 | Git | 全程版本管理与过程档案记录 |

### （二）需配置的外部资源

- Python 3.10+ 开发环境（推荐 Anaconda 管理虚拟环境）
- CUDA 环境（如使用 GPU 加速 LSTM 训练，可选）
- Git 与 GitHub 仓库
- Claude Code（核心 Harness 工具）+ GitHub Copilot（辅助补全）

### （三）数据集选择

**主数据集：MSM（Multi-Sensor and MTConnect）——用于刀具状态识别与智能诊断**

该数据集发表于 Nature Scientific Data（2026，DOI: 10.1038/s41597-026-07255-7），可在 Kaggle 下载（https://www.kaggle.com/datasets/manufuturetoday/multi-sensor-for-metal-milling-anomaly），采用 CC BY 4.0 许可。数据集集成声音、加速度计、电流互感器及 MTConnect 机床控制器数据，覆盖实验室实验（Hurco 铣床 imi_vm20i、imi_vmx30ui）和真实工业生产环境（Haas VF-10），共 3 台机床、69 次加工实验、2,575 个带标注的切片段。标注采用三级方案：0=正常切削（1,933 段）、1=加工异常（465 段）、2=刀具缺陷（177 段）。用于训练刀具状态三分类模型，并支撑跨机泛化研究与异常诊断知识库构建。

**辅助数据集：AI4I 2020 预测性维护数据集——用于剩余寿命（RUL）预测**

该数据集来自 UCI Machine Learning Repository（ID 601，https://archive.ics.uci.edu/dataset/601），采用 CC BY 4.0 许可，共 10,000 行、14 列。其含连续的「Tool wear [min]」磨损量字段及加工参数（空气温度、过程温度、转速、扭矩等），可直接构造 RUL 回归目标，用于训练 LSTM/随机森林寿命预测模型。论文：S. Matzka, "Explainable Artificial Intelligence for Predictive Maintenance Applications", 2020.

### （四）前端 UI 方案

前端采用 Streamlit 快速构建（优先方案）或 Vue 3 + Element Plus（如时间充裕）。主要界面包括：

- 设备总览看板：展示当前设备健康状态、最新状态识别结果、剩余寿命预测、告警数量统计
- 实时数据监测页：展示传感器实时数据波形图（振动、声音、电流等）
- 状态识别与趋势分析页：展示正常/异常/刀具缺陷三类状态识别结果及随时间的变化趋势
- 寿命预测页：展示刀具磨损随加工时间的变化曲线及 RUL 预测结果
- 诊断建议推送页：展示异常告警列表及对应的诊断建议与处置方案
- 历史数据查询页：支持按时间范围查询历史传感器数据和识别记录

### （五）后端与算法方案

后端采用 FastAPI 框架，提供以下 RESTful API 接口：

-- `GET /api/health`：健康检查
- `POST /api/predict/status`：调用刀具状态识别模型（正常/异常/刀具缺陷）
- `GET /api/samples/status`：状态识别原始特征样例（含真实标签）
- `POST /api/predict/rul`：调用剩余寿命预测模型
- `GET /api/samples/rul`：RUL 原始特征样例（含真实 RUL）
- `POST /api/diagnosis/query`：查询知识库获取诊断建议
- `GET /api/diagnosis/kb`：知识库全部条目
- `GET /api/history`：预测历史记录

算法模块独立封装，包含：

- 数据预处理模块：缺失值填充、异常值剔除、信号滤波、归一化、时序滑窗切分
- 特征提取模块：时域特征（均值、方差、峰值、峭度等）、频域特征（FFT 频谱特征）
- 状态识别模块：基于随机森林或 XGBoost，输出正常/异常/刀具缺陷状态标签
- 寿命预测模块：基于 LSTM，输出 RUL 预测值
- 超参数优化模块：遗传算法对模型超参数自动寻优
- 知识检索模块：基于 ChromaDB 向量数据库，实现 RAG 检索增强生成

> **实现适配说明（D7–D8 实际落地）**：因 AI4I 为截面表格数据（非时序）且环境未装 PyTorch / xgboost / chromadb，实际实现为——状态识别用随机森林 / 梯度提升（scikit-learn）；寿命预测用随机森林 / 梯度提升 / MLP（scikit-learn，已剔除磨损量本身防数据泄漏）；遗传算法从零手写（numpy）；知识检索用 TF-IDF + 余弦相似度（已抽象接口，可替换 ChromaDB）。详见 `src/README.md`。

## 四、技术路线

第一步：数据准备。解压 MSM 数据集、下载 AI4I 2020，进行数据清洗、预处理和特征工程，将处理后的数据存入 SQLite 数据库。

第二步：算法开发。实现遗传算法优化模块（超参数调优）、刀具状态分类模块（随机森林/XGBoost）、寿命预测模块（LSTM）、知识库检索模块（RAG）。各模块独立编写单元测试。

第三步：知识库构建。整理刀具状态、异常现象、原因和解决方案，构建结构化知识库，并导入 ChromaDB 生成向量索引。

第四步：后端开发。实现 FastAPI 应用，定义各 API 接口的路由、请求/响应模型，并封装对算法模块的调用。

第五步：前端开发。使用 Streamlit 或 Vue 构建交互式看板，对接后端 API，实现完整业务流程。

第六步：集成测试。运行完整的自动化测试套件，验证前后端联调和算法模块的正确性。

第七步：部署与演示。配置运行脚本，录制 3 分钟演示视频。

## 五、计划安排

| 阶段 | 天数 | 主要任务 |
|------|------|---------|
| D1-D2 | 2 | 工具配置、vibecoding 学习、选题调研（已完成）|
| D3-D5 | 2 | 选题与方案设计（含本文件撰写） |
| D6 | 1 | 数据集下载、清洗、预处理与入库 |
| D7-D8 | 3 | 算法模块开发（遗传算法调优→状态分类→寿命预测→知识库） |
| D8-D9 | 1 | 后端服务开发与前端界面构建 |
| D9 | 1 | 集成调试、自动化测试、报告撰写 |
| D10 | 1 | 答辩准备与演示 |

## 六、参考文献

[1] 张智海等.《制造智能技术基础》.北京：清华大学出版社，2022.

[2] Kim, E., Sim, Y., Li, A.S. et al. Multi-sensor and MTConnect dataset of metal cutting anomaly in milling from laboratory and industry settings. Scientific Data, 2026.

[3] Matzka, S. Explainable Artificial Intelligence for Predictive Maintenance Applications. 2020 IEEE 25th International Conference on Emerging Technologies and Factory Automation (ETFA), 2020.

[4] CutMind 项目: https://github.com/tangwan12/CutMind

[5] CNC-Tool-Wear-Detection: https://github.com/MAAykanat/CNC-Tool-Wear-Detection
