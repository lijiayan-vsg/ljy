# 3 分钟演示视频脚本

> 任务书要求：录制 3 分钟演示视频，展示系统可运行状态。建议按以下流程录制，全程录屏 + 口播。

---

## 时间轴（约 3 分钟）

### 0:00 - 0:20 开场与启动

- 口播：「这是 CNC 铣削刀具磨损全生命周期预测与智能诊断系统，采用 Streamlit + FastAPI 的 B/S 架构，覆盖智能优化、模式识别、知识工程三大技术方向。」
- 操作：双击 `run.bat`，展示后端（端口 8000）与前端（端口 8501）两个窗口启动。
- 打开浏览器访问 http://localhost:8501。

### 0:20 - 0:50 总览页

- 展示总览页：6 项关键指标（状态准确率 96.1%、样本量 12570、237 维特征等）。
- 展示「模型性能」：状态识别混淆矩阵与各类 F1、RUL 回归指标。
- 展示「系统流程」与三大技术方向卡片。

### 0:50 - 1:20 状态识别

- 左侧导航切换到「状态识别」。
- 选择一个样例（含真实标签），点击「预测状态」。
- 展示预测结果与真实标签「一致」，以及三类概率柱状图。

### 1:20 - 1:45 寿命预测

- 切换到「寿命预测」，选择样例或手动输入过程参数。
- 点击「预测 RUL」，展示预测 RUL 与剩余寿命进度条、预警提示。

### 1:45 - 2:10 智能诊断

- 切换到「智能诊断」，输入异常现象描述（如「刀具崩刃」）。
- 点击「诊断」，展示检索到的「可能原因」与「处置方案」。

### 2:10 - 2:30 历史记录

- 切换到「历史记录」，展示刚才的预测 / 诊断已自动入库、可追溯。

### 2:30 - 3:00 收尾

- 口播总结：「系统实现了从状态识别、寿命预测到智能诊断的完整业务闭环，三大技术方向均有实际落地，全部集成测试通过。」
- 展示 API 文档 http://127.0.0.1:8000/docs（可选）。

---

## 录制前检查清单

- [ ] 后端 `python -m uvicorn app.main:app --port 8000` 正常启动，无报错
- [ ] 前端 `python -m streamlit run web/app.py` 正常打开，无空白
- [ ] 已运行过至少一次预测，确保「历史记录」页有数据可展示
- [ ] 麦克风可用，屏幕录制软件已就绪
- [ ] 提前演练一遍，确保 3 分钟内讲完

---

## 口播要点（防超时）

- 每个功能页一句话带过，重点放在「状态识别结果 + 模型性能指标」上。
- 明确点出三大技术方向分别落在哪里（GA 寻优 / 特征分类 / 知识库检索）。
- 如实说明 RUL 的 R²≈0.09 是数据局限，体现严谨性。
