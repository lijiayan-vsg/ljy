# CNC 铣削刀具磨损全生命周期预测与智能诊断系统 —— 设计报告

**课程**：制造智能技术课程设计
**开发方法**：vibe coding（Claude Code 为核心 Harness 工具）
**完成时间**：2026-08

---

## 第一章 背景调研（研究意义）

### 1.1 问题背景

刀具是 CNC 铣削加工中最活跃的耗损要素之一。在批量生产中，刀具磨损直接决定工件的尺寸精度、表面质量与生产节拍：磨损超标的刀具会造成表面粗糙度恶化、尺寸超差甚至崩刃报废；而过度保守的「定期换刀」策略则会在刀具尚有寿命时提前报废，增加刀具成本与非加工时间。因此，刀具磨损状态的准确感知与剩余寿命的可靠预测，是制造过程降本增效的关键环节。

### 1.2 现状与局限

传统的刀具管理依赖人工经验巡检或固定换刀周期，存在明显局限：一是主观性强，依赖操作工经验，难以及时发现渐进式磨损；二是「一刀切」的固定周期既不能捕捉个体刀具差异，也无法应对工况波动；三是缺乏可追溯的量化记录，难以支撑后续工艺优化。

### 1.3 智能化趋势与本课题意义

随着传感器与机器学习技术的发展，基于数据的刀具状态监测（Tool Condition Monitoring, TCM）与预测性维护（Predictive Maintenance, PdM）成为主流方向。本课题构建的「状态识别—寿命预测—智能诊断」闭环系统，其意义在于：

1. **工程意义**：以多传感器信号自动、量化地感知刀具状态，替代人工经验判断；
2. **技术意义**：在一个系统中落地智能优化（遗传算法）、模式识别（时频特征 + 分类器）、知识工程（知识库 + RAG）三类课程核心技术，形成从感知到决策的完整链条；
3. **方法意义**：全过程采用 vibe coding 方法开发，验证了「AI 编程 + 人工审查」在制造智能应用原型开发中的可行性。

---

## 第二章 方案设计

### 2.1 功能需求分析

系统围绕刀具磨损全生命周期管理设计五个功能模块：

1. **数据采集与管理**：多传感器时序数据的整理、存储与索引；
2. **刀具状态识别**：时频域特征提取 + 分类器，输出「正常切削 / 加工异常 / 刀具缺陷」及置信度；
3. **剩余寿命预测**：基于历史磨损数据回归 RUL，临近阈值预警；
4. **智能诊断与推荐**：检测到异常或寿命低于阈值时，检索知识库推送原因与处置方案；
5. **可视化看板**：设备总览、状态识别、寿命预测、诊断推送、历史记录的交互界面。

### 2.2 技术方向覆盖

| 技术方向 | 对应课程章节 | 系统落地 |
|---------|-------------|---------|
| 智能优化技术 | 第 2 章 | 手写遗传算法对随机森林超参数自动寻优 |
| 模式与图像识别 | 第 3 章 | 多传感器时频域特征提取 + 随机森林三分类 |
| 知识工程 | 第 6 章 | 「状态—原因—方案」知识库 + TF-IDF 向量检索（RAG） |

### 2.3 系统架构

系统采用 B/S 架构，前后端分离，模块解耦：

```
浏览器（Streamlit 前端看板）
        │  HTTP/REST（8 个 API）
        ▼
FastAPI 后端服务（路由 / 校验 / 推理 / 入库）
        │
        ├── 算法模块（ga.py / classifier.py / rul.py / rag.py）
        └── SQLite（predictions.db，预测历史）
```

| 层级 | 技术选型 | 选型理由 |
|------|---------|---------|
| 前端 | Streamlit | 开发效率高，适合快速原型看板 |
| 后端 | FastAPI + Pydantic v2 | 轻量高效、自动生成 API 文档、与算法无缝集成 |
| 数据库 | SQLite（stdlib sqlite3） | 轻量易用，适合 demo 阶段 |
| 算法 | Scikit-learn（随机森林）+ 手写 GA + TF-IDF | 无需重型依赖，接口可替换 |

### 2.4 技术路线

第一步数据准备 → 第二步算法开发 → 第三步知识库构建 → 第四步后端开发 → 第五步前端开发 → 第六步集成测试 → 第七步部署演示。

> **实现适配说明**：因 AI4I 为截面表格数据（非时序）且环境未装 PyTorch / xgboost / chromadb，实际实现为——状态识别与寿命预测用随机森林（scikit-learn）；遗传算法从零手写（numpy）；知识检索用 TF-IDF + 余弦相似度（抽象接口，可替换 ChromaDB）。

---

## 第三章 数据资源构建

### 3.1 数据集选择

**主数据集 MSM（Multi-Sensor and MTConnect）**：发表于 Nature Scientific Data（2026），集成声音、加速度计、电流互感器及 MTConnect 机床控制器数据，覆盖 3 台机床、69 次加工实验、2575 个标注段。标注采用三级方案：0=正常切削、1=加工异常、2=刀具缺陷。用于状态识别与智能诊断。

**辅助数据集 AI4I 2020 预测性维护**：来自 UCI（ID 601），10000 行 × 14 列，含连续「Tool wear [min]」磨损量字段，直接构造 RUL 回归目标（RUL = max(0, 253 - Tool wear)），用于寿命预测。

两者均为 CC BY 4.0 许可，无私有敏感数据。

### 3.2 预处理流程

**MSM 预处理**（`data/preprocess_msm.py`）：

1. label 解析（start/end/label + 切削参数）；
2. mtc.csv 时间切片对齐；
3. 时域特征（7 类，31 列）+ 频域特征（FFT 3 频带，4 列）；
4. 近常数（低方差）列剔除；
5. Z-score 标准化（训练集拟合）；
6. 随机分层划分 train/val/test = 70/15/15。

最终得到 **2570 段 × 237 特征**（时域 7×31 + 频域 3×4 + 切削参数 6 + 方向 one-hot 2）。

**AI4I 预处理**（`data/preprocess.py`）：

1. 缺失值填充（数值中位数 / 类别众数）；
2. IQR 1.5 倍截断；
3. 派生特征（温差 Temp_diff_K、功率代理 Power_proxy）；
4. RUL 标签构造；
5. Type one-hot 编码；
6. Z-score 标准化；
7. 按 Machine failure 分层划分。

### 3.3 数据入库与索引

预处理后的特征矩阵、scaler 参数存入 `data/processed/`，并生成 `index.json` 数据索引（记录来源、规模、特征、划分、处理步骤），满足「小数据直接提交、大数据集给出有效链接并说明」的课程要求。MSM 原始数据 14.2 GB 压缩包不提交仓库，仅本地解压并通过 `data/README.md` 记录有效下载链接与许可。

---

## 第四章 系统实现

### 4.1 前端（Streamlit 看板）

`web/app.py` 实现 5 个功能页：

| 页面 | 功能 |
|------|------|
| 总览 | 关键指标、模型性能、系统流程、技术路线、数据来源 |
| 状态识别 | 选择样例 → 预测三分类 → 展示置信度与概率分布 |
| 寿命预测 | 使用样例 / 手动输入 → 回归 RUL → 预警 |
| 智能诊断 | 输入现象描述 → RAG 检索 → 展示原因与处置方案 |
| 历史记录 | 回看全部预测 / 诊断记录 |

界面采用「炭黑 + 琥珀」工业风格配色，指标数值用等宽字体呈现仪表读数感。

### 4.2 后端（FastAPI 服务）

`app/` 目录实现 8 个 RESTful API，核心设计：

- **端到端一致性**：后端接收「原始特征」，按训练时同一套规则（方向 one-hot、切削参数透传、信号 Z-score、RUL 派生特征）还原为标准特征向量，特征顺序取自模型 `feature_names_in_`，避免手工对齐出错；
- **样例验证**：`/api/samples/*` 把 test 集特征反向还原为原始特征，前端可对比真实标签验证链路；
- **输入校验**：缺失切削参数 / 信号特征返回 400（不静默填 0），float 字段拒绝 NaN/Infinity（422），历史 limit 有界（1–500）。

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/health` | 健康检查 |
| POST | `/api/predict/status` | 状态三分类 |
| GET | `/api/samples/status` | 状态识别样例（含真实标签） |
| POST | `/api/predict/rul` | RUL 预测 |
| GET | `/api/samples/rul` | RUL 样例（含真实 RUL） |
| POST | `/api/diagnosis/query` | RAG 诊断查询 |
| GET | `/api/diagnosis/kb` | 知识库条目 |
| GET | `/api/history` | 预测历史 |

### 4.3 数据库（SQLite）

`app/db.py` 使用 stdlib sqlite3，每次预测 / 诊断自动写入 `data/predictions.db`。连接启用 WAL 模式、超时与有限重试，规避 Windows 下偶发的文件锁只读错误。

### 4.4 算法模块一：遗传算法超参数寻优（智能优化）

`src/ga.py` 从零手写遗传算法（仅依赖 numpy），实现锦标赛选择、单点交叉、变异与精英保留策略。适应度函数采用 3 折交叉验证（分类用 macro-F1、回归用负 MAE），对随机森林超参数（n_estimators、max_depth、min_samples_leaf、max_features）自动寻优。

寻优结果：

- 状态分类：n_estimators=235、max_depth=20、min_samples_leaf=4、max_features=sqrt；
- RUL 回归：n_estimators=145、max_depth=26、min_samples_leaf=4。

### 4.5 算法模块二：刀具状态三分类（模式与图像识别）

`src/classifier.py` 对 237 维多传感器特征训练随机森林分类器。测试集 386 样本结果：

- **准确率 96.11%**，宏平均 F1 **0.9444**，加权 F1 **0.9617**；
- 混淆矩阵（行=真实，列=预测）：

| | 预测正常 | 预测异常 | 预测缺陷 |
|---|---|---|---|
| 真实正常 | 278 | 9 | 2 |
| 真实异常 | 3 | 66 | 1 |
| 真实缺陷 | 0 | 0 | 27 |

「刀具缺陷」类召回率 100%，「加工异常」类由于与正常切削信号存在部分重叠，F1 为 0.91，符合多传感器状态识别的典型表现。

### 4.6 算法模块三：剩余寿命（RUL）回归

`src/rul.py` 基于 AI4I 过程参数（Type + 空气/过程温度 + 转速 + 扭矩，派生温差/功率）回归 RUL。测试集结果：MAE **49.53** min、RMSE **60.79** min、R² **0.0933**。

> **关键发现**：AI4I 的磨损量是与加工参数解耦的独立随机变量，仅凭过程信号无法强预测 RUL（R²≈0.09）。这是数据特性而非模型缺陷，说明真实寿命预测须依赖与磨损强相关的传感器（如 MSM 的振动 / 主轴负载）。系统中已如实披露，并在 RUL 推理中剔除磨损量本身以防数据泄漏。

### 4.7 算法模块四：RAG 知识库检索（知识工程）

`src/rag.py` 构建「状态—原因—处置方案」知识库（`knowledge_base.json`），采用 TF-IDF 向量化 + 余弦相似度检索，对中文采用字符级 n-gram（2,3）提升匹配鲁棒性。检索接口已抽象，可替换为 ChromaDB 向量数据库。

---

## 第五章 测试与集成调试

### 5.1 自动化测试

`tests/test_api.py` 使用 FastAPI TestClient 直连后端，覆盖 6 项检查，全部通过：

| 检查项 | 结果 |
|--------|------|
| 健康检查 | ✓ |
| 状态识别端到端（样例预测 vs 真实标签） | 6/6 一致 ✓ |
| RUL 预测非负 | ✓ |
| NaN/Infinity 拒绝 | ✓ |
| RAG 诊断 Top-1 召回 | ✓ |
| 历史记录查询 | ✓ |

### 5.2 评审与缺陷修复

开发后期引入多维对抗式评审（4 个独立评审维度 + 对抗验证），确认并修复 3 个真实缺陷：

1. **切削参数缺失静默填 0**（会产生分布外输入的错误诊断）→ 改为 raise ValueError 返回 400；
2. **历史记录 limit 无约束**（limit=-1 可返回全表）→ 用 `Query(ge=1, le=500)` 约束；
3. **float 字段接受 NaN/Infinity** → `allow_inf_nan=False` + 校验器拒绝，并增加错误详情清洗处理器（解决 nan 无法 JSON 序列化的崩溃）。

### 5.3 集成联调

后端 uvicorn（端口 8000）+ 前端 Streamlit（端口 8501）联调通过，8 个 API 正常响应，前端 5 页均可走通完整业务流程，并提供 `run.bat` 一键启动脚本。

---

## 第六章 AI 使用披露

### 6.1 所用工具

- **Claude Code**：核心 Harness 工具，负责系统级架构设计、跨模块实现与集成；
- **DeepSeek-v4**：主要代码生成模型；
- **GitHub Copilot**：辅助代码补全。

### 6.2 关键 prompt 策略

1. **任务拆解**：将课程设计拆解为 D1-D10 各阶段子任务，逐阶段明确目标与验收标准；
2. **约束说明**：每个子任务明确技术栈、接口格式、数据表结构等约束（如「仅用 numpy 手写 GA」「特征顺序取自 feature_names_in_」）；
3. **上下文工程**：建立项目规则文件与 readme，让 AI 在修改前先通读现有代码结构，保持生成代码风格一致；
4. **规格驱动开发**：先撰写《需求规格说明书》，再让 AI 制定分步实现计划，逐步实现、逐步验收；
5. **自动化测试与人工审查**：AI 生成的代码须通过集成测试脚本验证，并逐行人工审查；
6. **多维对抗式评审**：用多代理工作流对系统做独立评审 + 对抗验证，剔除「看起来对实则不可触发」的误报，聚焦真实缺陷；
7. **调试与人工接管**：出错时采用回滚、缩小改动范围、构造最小复现等方式定位修复。

### 6.3 AI 出错案例及纠正过程

| 出错案例 | 现象 | 纠正过程 |
|---------|------|---------|
| GBK 编码错误 | 测试输出 ✓ 字符触发 `UnicodeEncodeError` | 在测试脚本顶部 `sys.stdout.reconfigure(encoding="utf-8")` |
| SQLite 只读错误 | Windows 文件锁偶发 `SQLITE_READONLY` | DB 连接加 WAL + 超时 + 有限重试 |
| NaN 序列化崩溃 | 422 错误详情含 nan 无法 JSON 序列化 | 增加 `_sanitize_nonfinite` 清洗 + 自定义异常处理器 |
| 静默填 0 缺陷 | 缺失参数被填 0 导致错误诊断 | 评审发现，改为显式抛错返回 400 |
| limit 无约束 | limit=-1 返回全表 | 评审发现，加 `Query(ge=1, le=500)` |
| httpx 拒绝 NaN | `json=` 参数无法传 NaN | 测试改用原始 content 发送 |
| 弃用 API | `use_container_width` 告警 | 改用 `width="stretch"` |
| 批处理换行 | run.bat 多行 if 块因 LF 换行失败 | 转为 CRLF 行尾 |

上述案例均通过「回滚 / 缩小改动 / 最小复现」策略定位，并经测试验证修复，印证了 vibe coding 中「AI 生成 + 人工审查 + 自动化测试」闭环的必要性。

---

## 第七章 总结与展望

### 7.1 完成情况总结

系统完整实现了「数据采集 → 状态识别 → 寿命预测 → 诊断推送」的业务闭环，覆盖智能优化、模式识别、知识工程三大技术方向，具备完整 B/S 架构（前端 / 后端 / 数据库 / 算法模块），状态识别测试集准确率 96.11%，全部集成测试通过，达到可现场演示状态。

### 7.2 已知局限

1. RUL 预测受 AI4I 数据特性限制（R²≈0.09），需引入与磨损强相关的传感器数据（如 MSM 振动 / 主轴负载）才能显著提升；
2. 状态识别「加工异常」类与正常切削存在部分重叠，F1 0.91，可通过更深层的时序特征或深度学习模型改进；
3. RAG 检索基于 TF-IDF 词面匹配，语义理解能力有限。

### 7.3 未来改进方向

1. 引入 LSTM / Transformer 等时序模型处理原始信号序列；
2. 知识库检索替换为 ChromaDB 向量数据库，支持语义检索；
3. 增加跨机床泛化验证与在线增量学习；
4. 前端补充实时波形图与趋势分析可视化。

---

## 附录：项目结构

```
├── readme.md / 选题说明.md / 方案设计.md / 需求规格说明书.md / 设计报告.md / 学习笔记.md
├── data/          # 数据来源、预处理程序、特征矩阵、索引
├── src/           # 算法模块（ga / classifier / rul / rag / knowledge_base）
├── app/           # FastAPI 后端（main / inference / samples / db / routers）
├── web/           # Streamlit 前端
├── tests/         # API 集成测试
├── models/        # 模型产物（*.joblib 不提交，*.json 指标提交）
├── prompt/        # AI 提示词追溯记录
└── run.bat        # 一键启动脚本
```
