# 需求规格说明书

**项目名称**：CNC 铣削刀具磨损全生命周期预测与智能诊断系统
**课程**：制造智能技术课程设计
**开发方法**：vibe coding（Claude Code 为核心 Harness 工具）

---

## 一、引言

### 1.1 编写目的

本说明书定义「CNC 铣削刀具磨损全生命周期预测与智能诊断系统」的功能需求、接口需求、数据需求与验收标准，作为系统开发、测试与验收的统一依据。本说明书采用规格驱动开发方式，后续算法模块、后端服务与前端界面的实现均以本文档为基准逐步实现并逐项验收。

### 1.2 项目背景

刀具磨损是 CNC 铣削加工中影响加工质量与生产效率的关键因素。刀具磨损超标若未被及时发现，会导致工件表面质量下降、尺寸超差甚至工件报废；过度保守地提前换刀则造成刀具成本浪费。因此，构建一套能自动识别刀具状态、预测剩余使用寿命并在异常时给出诊断建议的系统，具有明确的工程意义。

### 1.3 术语定义

| 术语 | 说明 |
|------|------|
| MSM | Multi-Sensor and MTConnect 金属铣削异常数据集，含振动、声音、电流及 MTConnect 控制器数据 |
| AI4I 2020 | UCI 预测性维护数据集（ID 601），含连续 Tool wear 字段 |
| RUL | Remaining Useful Life，剩余使用寿命 |
| RAG | Retrieval-Augmented Generation，检索增强生成 |
| GA | Genetic Algorithm，遗传算法 |
| 状态三分类 | 正常切削 / 加工异常 / 刀具缺陷 |

---

## 二、项目目标

系统以多传感器信号（振动、声音、切削电流、机床控制器数据等）为输入，实现以下三项目标：

1. **刀具状态识别**：对多传感器信号进行时频域特征提取与分类，自动识别「正常切削 / 加工异常 / 刀具缺陷」三类状态。
2. **刀具剩余寿命预测**：基于历史磨损数据建立 RUL 回归模型，在接近报废阈值前预警。
3. **智能诊断与决策支持**：构建「状态—原因—处置方案」知识库，检测到异常时通过 RAG 自动推送诊断建议。

三个目标分别对应课程三大技术方向：**智能优化技术**（遗传算法超参数寻优）、**模式与图像识别**（时频域特征 + 分类器）、**知识工程**（知识库 + RAG），形成从「感知 → 预测 → 决策」的完整业务闭环。

---

## 三、系统范围与架构

系统采用 B/S 架构，前后端分离：

```
浏览器（Streamlit 前端看板）
        │  HTTP/REST
        ▼
FastAPI 后端服务（业务逻辑 + 算法调用）
        │
        ├── 算法模块（GA / 状态分类 / RUL 回归 / RAG 知识库）
        └── SQLite 数据库（预测历史记录）
```

| 层级 | 技术选型 | 职责 |
|------|---------|------|
| 前端 | Streamlit | 交互式看板，走通完整业务流程 |
| 后端 | FastAPI + Pydantic v2 | RESTful API，封装算法调用与数据校验 |
| 数据库 | SQLite（stdlib sqlite3） | 预测 / 诊断历史记录 |
| 算法 | Scikit-learn（随机森林）+ 手写 GA + TF-IDF | 状态分类、RUL 回归、超参数寻优、知识检索 |

---

## 四、功能需求

### 4.1 刀具状态识别（模式与图像识别方向）

**描述**：输入一条加工段的多传感器原始特征（方向 + 6 项切削参数 + 229 个信号特征，共 237 维），输出三分类状态标签及各类置信度。

**输入**：
- `direction`：切削方向（X / Y / Z）
- `params`：切削参数（主轴转速、进给速度、切削深度等 6 项）
- `signals`：信号特征（229 项，训练集拟合的 Z-score 标准化后特征）

**输出**：
- `label`（0/1/2）、`label_name`（正常切削 / 加工异常 / 刀具缺陷）
- `confidence`（预测置信度）
- `probabilities`（三类概率分布）

**验收标准**：测试集 386 样本准确率 ≥ 95%，样例预测与真实标签一致。

### 4.2 剩余寿命（RUL）预测

**描述**：输入产品类型 Type 与 4 项加工过程量（空气温度、过程温度、转速、扭矩），回归预测剩余使用寿命 RUL（单位 min），并派生温差 / 功率代理特征。

**输入**：`type`（L/M/H）、`air_temperature`、`process_temperature`、`rotational_speed`、`torque`。

**输出**：`rul`（≥ 0）、`warning`（临近磨损上限时提示）。

**验收标准**：RUL 非负；RUL < 磨损上限 253 min 时给出预警。已知数据局限：AI4I 的磨损量与加工参数解耦，R²≈0.09，需在界面如实披露。

### 4.3 智能诊断（知识工程方向）

**描述**：输入异常现象描述文本，检索知识库返回 Top-K 条「状态—原因—处置方案」。

**输入**：`query`（中文现象描述）、`top_k`（1–5）。

**输出**：匹配条目的 `status`、`keywords`、`cause`、`solution` 及相似度。

**验收标准**：中文关键词查询 Top-1 正确召回对应状态。

### 4.4 可视化看板

前端 5 个功能页：总览（指标、模型性能、系统流程、数据来源）、状态识别、寿命预测、智能诊断、历史记录。

### 4.5 历史记录

每次预测 / 诊断自动写入 SQLite，前端可回看与导出。

---

## 五、接口需求（RESTful API）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/health` | 健康检查 |
| POST | `/api/predict/status` | 刀具状态三分类 |
| GET | `/api/samples/status` | 状态识别原始特征样例（含真实标签） |
| POST | `/api/predict/rul` | RUL 预测 |
| GET | `/api/samples/rul` | RUL 原始特征样例（含真实 RUL） |
| POST | `/api/diagnosis/query` | RAG 诊断查询 |
| GET | `/api/diagnosis/kb` | 知识库全部条目 |
| GET | `/api/history` | 预测历史记录 |

---

## 六、数据需求

| 数据集 | 用途 | 规模 | 许可 | 处理方式 |
|--------|------|------|------|---------|
| MSM 多传感器 | 状态识别 + 智能诊断 | 2570 标注段 × 237 特征 | CC BY 4.0 | 公开大数据集（14.2 GB 压缩），不提交仓库，经 `data/preprocess_msm.py` 处理为特征矩阵 |
| AI4I 2020 | RUL 预测 | 10000 条 × 14 列 | CC BY 4.0 | 提交下载链接（原始数据不入库，本地 `data/download_data.py ai4i` 获取） |

---

## 七、非功能需求

1. **可运行性**：项目通过自带自动化测试（`tests/test_api.py`），可现场演示。
2. **输入健壮性**：拒绝缺失切削参数 / 信号特征（返回 400）、拒绝 NaN/Infinity（返回 422），历史查询 limit 有界（1–500）。
3. **可复现性**：模型产物可由脚本重建，特征顺序取自模型 `feature_names_in_` 保证一致性。
4. **可维护性**：算法模块独立封装，接口可替换（如 RAG 检索层可换 ChromaDB）。
5. **安全性**：本地配置 / 凭据通过 `.gitignore` 排除，不入库。

---

## 八、验收标准汇总

| 编号 | 验收项 | 标准 | 实测 |
|------|--------|------|------|
| A1 | 状态识别准确率 | ≥ 95% | 96.11% |
| A2 | 状态识别宏平均 F1 | — | 0.9444 |
| A3 | RUL 预测 | 非负且可预警 | 通过 |
| A4 | RAG 诊断 | Top-1 正确召回 | 通过 |
| A5 | 集成测试 | 全部通过 | 6 项通过 |
| A6 | 架构完整 | 前端 / 后端 / 数据库 / 算法齐全 | 满足 |
| A7 | 技术方向覆盖 | ≥ 3 个方向且有实际作用 | 3 个方向 |
